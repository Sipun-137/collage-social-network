// import UpdateForm from "@/app/components/UpdateForm";


export default function Page({ params }: { params: { id: string } }) {
  return (
    <div>{params.id}
    {/* <UpdateForm {...params}/> */}
    this is the secction for the update the application post data
    </div>
    
  )
}
